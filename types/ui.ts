/**
 * UI state types used across the application.
 */

/** Tab identifier for main navigation */
export type TabKey = "finalists" | "compare";

/** Confirmation modal type */
export type ConfirmKey = "pick" | "none" | null;
